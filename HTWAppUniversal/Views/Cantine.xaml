<Page
    x:Class="HTWAppUniversal.Views.Cantine"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HTWAppUniversal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <RelativePanel>
            <Grid x:Name="headline" Grid.RowSpan="1" Height="48px" Background="RoyalBlue" Width="2000">
                <TextBlock Text="Mensa" FontSize="16" Margin="4 0 0 0" VerticalAlignment="Center" Foreground="White"/>
            </Grid>
            <TextBlock x:Name="ph0" RelativePanel.Below="headline"/>
            <ListView x:Name="mainView" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="ph0"/>
        </RelativePanel>
    </Grid>
</Page>
